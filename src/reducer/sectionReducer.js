export function sectionReducer(state, action) {
    if(action.type === "section_2") {
        return {
            ...state,
            section_2: true
        }

    }
    if(action.type === "section_3") {
        
    }
    if(action.type === "section_4") {

    }
}